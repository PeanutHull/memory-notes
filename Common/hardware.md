### 硬盘
1. 硬盘
   - 分类
     1. HDD：机械硬盘，2.5英寸，3.5英寸
     1. SSD：固态硬盘，适合大量随机io场景
     1. PCI-E SSD：性能。成本更高，单独接口
     1. JBOD：just bunch of disks，一堆普通磁盘，类似raid
     1. RAID：磁盘阵列，磁盘阵列由阵列卡管理
   - 接口类型
     1. 物理
        - IDE/ATA：Integrated Drive Electronics 电子集成驱动器，也称作"ATA硬盘"或"PATA硬盘"
          1. IDE是一种计算机系统接口，主要用于硬盘和CD-ROM，本意为"把控制器与盘体集成在一起的硬盘"
        - M.2：Intel推出的替代mSATA的接口规范，体积小，散热不好，曾用名NGFF
          1. 支持两种通道总线，SATA总线和PCI-E总线。即可以是SATA SSD也可以是NVMe（PCIe）SSD
          1. 理论速度：PCIe 3.0X2是16Gb/s，PCIe 3.0X4是32Gb/s，PCIe 4.0X4是64Gb/s
        - U.2：数据中心、企业级使用，支持热插拔，散热不错，只能是PCIe 4lane
        - AIC：企业级使用，支持PCIe 4lane/8lane，带宽上限更高
        - FC：Fibre Channel FC光纤技术，是ANSI为网络和通道I/O接口建立的一个标准集成。支持HIP PI 、IPI、SCSI、IP、ATM等高级协议
     1. 高速信息协议
        - SAS：Serial Attached SCSI 串行连接SCSI，和SATA一样采用串行提高速度，支持双向全双工模式，多用于企业级、磁盘阵列上，比SATA快，更可靠，12Gb/s
        - SATA：Serial ATA，主流SATA3，理论传输带宽6Gbps，传输速度600MB/s，主流但是因为速度上限面临淘汰
          1. mSATA：mini-SATA，适用于便携设备
          1. AHCI：高级主机控制器接口，来激活高级SATA功能如原生指令队列、热插拔
        - PCIe：之前是硬盘数据先到内存，再到cpu，现在直接通过总线和cpu连接，更快，速率也更高，支持多个lane，主流PCIe 3.0x4 lane，3500MB/s
     1. 传输层协议
        - SCSI：Small Computer System Interface 小型计算机系统接口，历史悠久，应用广范，U盘/SAS盘手机的UFS，多任务、带宽大、CPU占用率低及热插拔等
        - NVMe：NVM Express，通过PCIe访问可以大幅降低延迟，突破了SATA最大吞吐量的限制
   - 性能指标：分随机/顺序，读/写；一般用4k的大小来测
     1. iops：每秒的io次数，机械硬盘理论计算方式为：1000/(寻道时间+旋转延迟)
        - SSD
          1. 随机读600k(旗舰的1000)，随机写500k(旗舰的700)
     1. 读写带宽
        - SSD
          1. 顺序读：3100MB(旗舰的7000)，顺序写1800MB(旗舰的3000)
     1. 读写延时
        - SSD：随机读70us，随机写9us
   - 速度对比
     1. 内存顺序读写最快
     1. 内存随机读写和SSD/机械顺序读写速度差不多，磁盘访问的成本是内存的十万倍
     1. 磁盘的顺序读写有时比内存快10倍，100倍
   - 测试工具：fio、dd，window用IOMeter
   - 实践：每秒io数超过200的时候性能就会不好
   - 挑选方面：通电时间和坏块情况
   - 价格
     1. 机械
        - 6T：企业级1800，日常pc900
        - 18T：企业级5600
     1. 固态
        - 3.85T：企业级9000，SATA3的4000
        - 7.68T：企业级20000
   - 特性
     1. 氦气封装
1. HDD：机械硬盘
   - 组成：盘片、磁头、磁道、扇区、柱面
     1. 多盘片堆叠
     1. 一个盘片，一个磁头
     1. 盘片有一圈圈的磁道
     1. 扇区是硬盘的最小单元，也是磁道的组成，硬盘以扇区为单位管理和传输数据，一般为512byte
     1. 从上到下相同的磁道组成类似圆柱的柱面
   - 数据交换时间：一次io要10ms左右。500-MIPS的cpu每秒可以执行5亿条指令，执行一次I/O的时间可以执行40万条指令。数据库动辄百万级甚至千万级的数据，每次9ms的时间，太慢
     1. 寻道时间：磁头移动到数据所在磁道的时间，7200转的9ms，10000的6ms，15000的4ms
     1. 平均旋转延迟：扇区移至磁头下方的时间，7200转的4ms(1/120/2=4.17ms)，10000的3ms，15000的2ms
     1. 数据传输：0.nms，会忽略不记
   - 常见接口类型：SATA、SAS
1. SSD：Solid State Drives，固态硬盘，用固态电子存储芯片阵列而制成的硬盘。极低的寻道时间、存取时间，比机械快n个量级
   - ssd盘文件最大不超过总空间的85%,以避免空间占比可能带来的性能下降
   - UFS：硬盘标准，硬盘的存储读取速度
   - PCIe SSD：PCIe 4.0 SSD，读最高5GB/s，写最高4.4GB/s，随机读写750KIOPS、700KIOPS
   - 常见接口类型：SATA、MSATA、M.2
1. SSHD：混合硬盘，HDD+SSD
1. RAID
   - 认识：廉价冗余磁盘阵列，之后称为独立磁盘冗余阵列，由多个独立的高性能磁盘驱动器组成的磁盘子系统，从而提供比单个磁盘更高的存储性能和数据冗余的技术，是一种多磁盘管理技术。至少3块硬盘，操作系统可以看作一个硬盘
     1. 大容量：可达PB级
     1. 高性能：可形成聚合I/O性能
     1. 高可靠：若干磁盘出错时，不导致数据丢失，不影响系统连续运行
     1. 可管理性
   - 技术
     1. 镜像
     1. 数据条带
     1. 数据校验
   - 等级：RAID0-6级，性能、可靠、成本三个方面的均衡
     1. RAID0：数据平均分配存储，性能提升倍数和盘数量相同，一块坏了所有都不能读取，软件可实现
     1. RAID1：磁盘镜像，多磁盘间冗余数据，即再复制一份数据到另外的磁盘，更换硬盘需要长时间同步数据，读性能好，软件可实现
     1. RAID5：分布式奇偶校验磁盘阵列，只需要额外一块磁盘，将数据分散到多个磁盘，任何一个盘失效可从奇偶校验块重建，两块一起坏无法读取，常用
        - 随机写比较慢：因为实际要发生两次读、两次写
        - 随机读、顺序读很快，不需要计算校验位，适合读为主，放到从服务器上
        - 重建慢
     1. RAID10：分片的镜像，对磁盘先做1再对两组1再做0，读写都有良好性能，对比5重建更快更简单，数据库性能最好，常用
   - 对比：![avatar](../images/raid_compare.png)
   - 实现方式
     1. 软RAID：没有专用控制、I/O芯片，操作系统支持
     1. 硬RAID：有专用控制、I/O芯片、阵列缓冲，支持热交换技术，在系统运行下更换故障磁盘
   - 其他：光盘机、磁带机、磁带库
1. 卷管理系统：操作系统层面
   - Windows的动态磁盘
   - UFS：FreeBSD
   - ZFS：Solaris
   - LVM
     1. 认识：Logical Volume Manager 逻辑卷管理，linux内核里的，通过在磁盘和分区之上建立"逻辑"卷，实现磁盘分区管理、弹性大小的工具。因为固定大小分区重新调整非常麻烦，lvm非常灵活性
        - 不需要重启就可以让内核知道分区的存在
        - 随意将新的硬盘添加到lvm
        - 直接扩展已经存在的逻辑卷
     1.  架构
        - Disk n：多个硬盘
          1. 物理分区
          1. PV：physical volume 物理卷
        - VG n：volume group 卷组，n个物理卷可以创建卷组
        - LV n：logical volume 逻辑卷，基于卷组创建逻辑卷，卷组有空间，就可以随意创建逻辑卷
        - File system：文件系统在逻辑卷上创建，然后可以在操作系统挂载和访问
     1. 命令
        - pvcreate、pvdisplay
        - vgcreate、vgdisplay、vgremove：物理卷相关
        - lvcreate、lvdisplay、lvresize：逻辑卷相关
        - resize2fs：扩展文件系统，更新ext4信息
### 存储
1. 存储类型
   - 块
     1. 认识：将裸磁盘空间整个映射给主机使用，用户是可以读写块设备的软件系统，如文件系统、备份软件、分区软件，支持直接读写块设备的软件如数据库一般很少用
        - 块设备被挂载使用
        - 主要操作对象是磁盘，没有文件和目录树的概念
        - 传统文件系统是直接访问存储数据的硬件介质的，介质不关心也无法关心这些数据的组织方式以及结构，因此用的是最简单粗暴的组织方式。所有数据按照固定大小分块，每一块赋予一个用于寻址的编号。机械硬盘一块就是一个扇区，老式硬盘512字节大小，新硬盘4K字节。老式硬盘用柱面-磁头-扇区号（CHS，Cylinder-Head-Sector）组成的编号进行寻址，现代硬盘用一个逻辑块编号寻址（LBA，Logical Block Addressing）。块设备通常可以划分为多个逻辑块设备，就是硬盘分区
        - 至于哪些块组成一个文件，哪些块记录的是目录/子目录信息，这是文件系统的事情。不同的文件系统有不同的组织结构
        - 把多个物理块设备组合成一个逻辑块设备，如RAID，JBOD，Windows的动态磁盘、Linux的LVM
        - 块存储是排它的，某个逻辑块被一台客户端挂载后，其它客户端就无法访问上面的数据
        - raid和lvm在更底层，对数据提供保护，上层操作系统无法感知是物理磁盘还是逻辑磁盘，同时由于是多个物理磁盘可以并行写入，效率倍升
     1. 设备：磁盘阵列、硬盘
     1. 优点
        - 通过Raid与LVM等手段，对数据提供保护
        - 多块廉价硬盘组合，提高容量
        - 多块磁盘组合的逻辑盘，提升读写效率
     1. 缺点
        - 采用SAN架构组网时，光纤交换机，造价成本高
        - 主机之间无法共享数据
     1. 场景
        - docker容器、虚拟机磁盘存储分配
        - 日志存储
        - 文件存储
   - 文件
     1. 认识：文件可以共享了，文件管理功能已经由文件存储自己搞定了，用户是自然人，主要操作对象是文件和文件夹，兼容POSIX接口
     1. 设备：架上FTP、NFS等服务的服务器，就是文件存储的一种
     1. 优点
        - 造价低，随便一台机器就可以
        - 方便文件共享
     1. 缺点
        - 读写速率低
        - 传输速率慢
     1. 场景
        - 有目录结构的文件存储
        - 日志存储
   - 对象
     1. 认识：多台服务器内置大容量硬盘，安装上对象存储管理软件，可以共享的大容量读写服务，使用一个统一的底层存储系统，利用块存储的直接访问和文件存储的路径划分两个优点，主要操作对象是对象
        - 对象存储支持的访问接口基本都是restful接口、而分布式文件系统提供的POSIX兼容的文件操作接口
        - 分布式文件系统文件组织方式为目录树、对象存储采用的则是扁平的组织方式；对象存储不支持随机读取和写入，put和get操作都是针对的整个文件
        - 对象存储将元数据独立出来了
        - 基于哈希环之类的实现，可以非常简单的扩展到超大规模，因此非常适合数据量大、增速又很快的视频、图像等
     1. 设备：内置大容量硬盘的分布式服务器(swift, s3)
     1. 优点
        - 具备块存储的读写高速
        - 具备文件存储的共享等特性
     1. 场景
        - 图片存储
        - 视频存储
   - 分布式：多台服务器提供服务，往往可以同时提供文件存储、块存储和对象存储
1. 集中式存储
   - DAS：Direct Attached Storage 直接连接存储，将外置存储设备通过连接电缆直接连接到计算机上，是nas的前身
     1. 这种的一般数据和操作系统都未分离，也可以采用磁盘阵列
   - NAS
     1. 认识：network attached storage，网络附属存储，是可进行数据传输的大容量、高可靠网络协议的、可共享的专用数据存储服务器，存储效率高于服务器，成本更低
        - 支持多种协议，如NFS、CIFS、FTP、HTTP，主要分为CIFS和NFS，存储厂商在主导，如华为的OceanStor系列
        - 软件厂商，FreeNAS、OpenMediaVault、NAS4Free
     1. 组成
        - 硬件：nas箱、nas设备等，本质就是服务器，nas箱仅发送和接收两种请求：数据存储和文件共享
        - 软件：软件部署在轻量级操作系统(通常嵌入在硬件中)上
        - 协议：nas箱会根据数据传输协议进行格式化，以便按照相应的标准方式在设备间发送数据
     1. 特点
        - 横向扩展，增加存储容量就和增加硬盘一样简单，无中断扩容
        - 高性能、易于设置：提前通过简化脚本来交付，或以简化版操作系统的预安装设备的形式来交付
        - 可访问性：所有设备可访问
        - 容错性：可以对nas进行格式化，以支持raid
   - SAN
     1. 认识：Storage Area Network 存储区域网络，通过光纤集线器/光纤路由器/光纤交换机等连接设备将磁盘阵列、磁带等存储设备与服务器连接起来的高速专用于数据存储的子网/区域网络，有存储专用处理器，最高100Gbps的Ethernet，32Gbps的FC光口，1200万iops等，支持各种网络协议
        - 主要分为IP SAN和FC SAN
        - 允许任何服务器连接到任何存储阵列，并光纤交换技术高带宽，这样才能灵活使用
        - 十多年发展已很成熟，成为业界的事实标准，企业级的存储方案。突破了SCSI标准的限制
        - 满足从容量、性能、可用性、数据安全、数据共享、数据整合等方面的应用，对存储提出的要求，内部总线架构将逐渐走向消亡，形成交换式(fabrics)网络化发展方向的趋势，数据存储部分已经独立出来。未来将是I/O和计算处理的进一步分离，形成数据存储、计算处理、I/O吞吐三足鼎立
     1. 比较
        - 和nas都是使用raid的冗余存储系统
        - san存储设备通过光纤连接，而nas存储设备通过tcp/ip网络连接，san高级方案，nas家庭或者小型企业，san使用SCSI光纤通道更复杂
        - 和nas的边界正在模糊，都在发展
        - Cloud SAN能提供更大功能
   - 比较
     1. 和集中式存储划分也没那么清晰，如OceanStor的统一存储，即做到了SAN和NAS层面的统一，也做到了文件存储和块存储层面的统一
     1. 外部文件设备加载到服务器上的方法
     1. san的随机读写甚至不如本地RAID
1. 分布式存储：开源文件系统在主导，如ceph
### CPU
1. 认识：1GHz的cpu一秒执行大约10亿次。分为物理cpu、逻辑cpu(物理cpu数量*每颗cpu核数)。3GHz的大多数简单指令要一个时钟周期即三分之一ns
1. 组成
   - 控制器
     1. 程序计数器
     1. 时序发生器
     1. 指令译码器：将指令翻译为电平
     1. 各种寄存器
   - 运算器
     1. 数据缓冲器
     1. ALU
     1. 通用寄存器
     1. 状态字寄存器
1. 指令
   - 认识
     1. 数据和指令是分开区域存放的，存放指令区域的地方称为「正文段」
   - 指令集：约定的cpu执行的方式和对应二进制码的关系，
     1. MIPS指令是一个32位整数，高6位代表着操作码，剩下的 26 位不同指令类型所表示的内容也就不相同，如R、I、J
        - R指令，用在算术和逻辑操作，里面由读取和写入数据的寄存器地址
        - I指令，用在数据传输、条件分支等
        - J指令，用在跳转，26位都是跳转后的地址
   - 类型
     1. 数据传输
     1. 运算
     1. 跳转
     1. 信号
     1. 闲置
   - 指令周期
     1. 认识：cpu执行指令的形式，也叫指令流水线，即cpu的工作就是一个周期接着一个周期，周而复始
        - 一个指令分成多个步骤，由专门电路执行，为了一条指令需要的CPU时钟周期数尽可能的少
     1. 步骤
        - Fetch：取得指令
        - Decode：指令译码
        - Execution：执行指令
        - Store：数据回写
     1. 步骤：一个程序执行时cpu根据程序计数器里的内存地址，从内存把需要执行的指令读取到指令寄存器里面执行，然后根据指令长度自增，开始顺序读取下一条指令
        - cpu读「程序计数器」值(指令的内存地址)，然后cpu的「控制单元」操作「地址总线」指定需要访问的内存地址，接着通知内存设备准备数据，数据准备好后通过「数据总线」将指令数据传给cpu，cpu收到内存传来的数据后，将这个指令数据存入到「指令寄存器」
        - cpu分析「指令寄存器」中的指令，确定指令的类型和参数，计算类型交给「逻辑运算单元」运算；存储类型交由「控制单元」执行
        - cpu执行完指令后，「程序计数器」的值自增，表示指向下一条指令。这个自增的大小，由cpu的位宽决定，如32位CPU指令是4字节因此「程序计数器」的值自增4
   - 执行速度
     1. 程序cpu执行时间 = cpu时钟周期数（CPU Cycles）* 时钟周期时间（Clock Cycle Time）
1. 运行原理
   - 有小工作区由几个寄存器组成，每个寄存器可以存储数字，有的存储下一个指令的内存地址，用于获取和更新下一个指令，获取指令后在另一个寄存器存储该指令，并更新第一个寄存器的内存地址。CPU操作的每个操作数和操作结果，都由寄存器来暂时保存，最后才写入到内存或从内存中读出。X86也就是十几个寄存器
     1.  存储的所有内容都是数字，计算机程序最终以数字指令码，即机器语言来表示
1. 概念
   - 系统自举：就是系统刚启动，自己把自己需要的代码载入，然后再执行更多的逻辑
   - 用户空间、内核空间：内存划块，用户进程可以进入内核空间，相当于每个进程的虚拟4G内存，权限低不能访问系统内存和设备
   - 时间
     1. 时间组成：user + system + nice + idle + iowait + irq + softirq
     1. 时钟时间 ＝ 阻塞时间 ＋ 就绪时间 ＋ 运行时间
     1. 运行时间 = CPU时间 = 用户CPU时间 + 系统CPU时间
     1. 用户CPU时间：用户进程获得CPU后在用户态执行的时间
     1. 系统CPU时间：用户进程获得CPU后在内核态的执行时间
     1. 实际时间：多线程或者子进程情况下，并行执行可能小于cpu时间，io密集型存在等待可能大于cpu时间
   - 时钟
     1. 时钟周期：晶体振荡器在1秒时间内产生的时钟脉冲个数就是时钟周期的频率
     1. 时钟滴答：通道0的计数器减到0值时产生一次时钟中断，即时钟滴答，计数器的初始值决定了滴答的时间间隔
1. 分类
   - 时限：
     1. 实时操作系统
        - 硬实时
        - 软实时
     1. 分时操作系统：CPU的时间被平均分成时间片，内核的主要工作就是分配CPU时间给各个进程使用
   - 体系架构：也是指令集
     1. X86
        - 认识：复杂指令集，CISC，主要由Intel、AMD开发，由78年发布16位的8086处理器而来
          1. amd64：即x86-64或x64，由amd在99年设计，新增了64位寄存器，并兼容早期的16位和32位软件，可指代amd64和intel64
     1. PowerPC：IBM，精简指令集，RISC
     1. ARM：ARM
   - 位宽
     1. 32
     1. 64
1. wiki
   - 高并发场景核数更重要，cpu密集型频率更重要
   - 4核8线程：就是一个物理核虚拟成的两个逻辑处理单元，即线程，可以同时运行8个计算
   - 芯片组：cpu和其他设备沟通的桥梁
   - 位宽：硬件的64和32指的是cpu的位宽，软件的64和32指的是指令的位宽
   - 寄存器：寄存器——缓存(使用快速的SRAM)——内存(DRAM)，寄存器临时存放，缓存暂时存放
   - 图灵机
     1. 认识：读取纸带格子中的内容，然后交给控制单元识别字符是数字还是运算符指令
     1. 组成
        - 纸带记录数据或指令
        - 读写头上有一些部件，比如存储单元、控制单元以及运算单元
   - 冯诺依曼模型
     1. 认识：1945年冯诺依曼和其他计算机科学家们提出计算机具体实现的报告，其遵循了图灵机的设计，而且还提出用电子元件构造计算机，并约定了用二进制进行计算和存储。计算机基本结构为5部分：中央处理器、内存、输入设备、输出设备、总线
### 内存
1. 认识：可随机访问，可读写。可理解为有永久绑定的特定地址的单元格
   - 大小：理论上无限大
   - 位宽：有宽度限制，8位/16位，逻辑内存是任意的，不影响内存操作
   - 结构
     1. 哈佛结构：数据和代码分开存放
     1. 冯诺依曼结构：数据和代码放在一起
   - 组成
     1. 大小
     1. 频率
        - MT/s：即Mbps，速率，多少次数据传输
        - MHz：时钟频率
     1. 时序
1. 分类
   - SRAM
   - DRAM
     1. SDRAM
        - DDR、DDR1...
1. ECC内存：六通道
1. DIMM插槽
1. 内存控制器：cpu只从内存控制器取数据
1. 特性
   - 虚拟内存
     1. 认识：使程序认为它拥有连续完整的地址空间，实际是多个物理内存碎片，甚至部分暂存在硬盘上，可以寻址超出物理空间的地址
        - 程序运行进行两次内存地址映射
          1. 虚拟内存空间
          1. 物理内存空间：需要软硬件配合，MMU利用操作系统的内存管理模块的虚拟/物理映射记录表转化为实际物理地址
        - cpu用的也是虚拟地址，之后MMU会转为物理地址后去访问
        - 内存分为用户空间、高地址的内核空间
     1. 作用
        - 地址空间不隔离，各个程序相互改地址
        - 程序运行时候的地址不确定，每次运行地址会变
        - 内存使用率低下，尽量用满内存，不能浪费
     1. 分类
        - windows的虚拟内存
        - linux的交换空间：swap space
          1. 页：page，一般为4k，操作系统操作内存的最小单元。范围512Byte~8K，越大，浪费越多，但调度效率越高，不用频繁分配和释放
          1. 页框：物理内存的块
          1. 页表：即映射记录表，页和页框的对应关系，放在物理内存中，由操作系统维护
          1. 块表：硬件寄存器中存放一个页表，加快访问速度。因为发现系统一旦访问某页那么就会在一段时间内稳定地工作在这个页上
          1. 多级页表：对页表也进行分页存储，建立记录页表页首地址的页目录表，linux使用三级页表
          1. 大页机制：把页的大小设为MB或者GB，将映射关系全部存于TLB
          1. 内存回收：分为可随时释放的可回收页面
          1. compaction：内存使用一段时间会支离破碎，需要分配较大的物理连续内存区域时，由kcompactd守护进程移动到合适位置
          1. OOM killer：内核主动kill，以释放内存
          1. 页高速缓存：把磁盘中的数据缓存到物理内存中，把对磁盘的访问变为对物理内存的访问
     1. wiki
        - MMU：存储管理单元，承担内存管理任务的硬件部分，通过CPU将线性地址转换成物理地址，过程叫table walk
        - 映射记录表
          1. 每个程序各自都有一个大小与处理器寻址空间相等的虚拟内存空间
          1. 在一个具体时刻处理器只能使用其中一个程序的映射记录表，保证了程序的虚存空间互不相扰
          1. 使用程序映射表可方便地实现物理内存的共享
   - 内存对齐：为了提高内存访问效率，不用多个总线周期来读取数据。无法跨两个数据宽度进行IO
   - 内存读取数据的局部预读性，每一次I/O读取的数据称为一页。页大小跟操作系统有关，一般为4K
1. 发展历史：有统一标准协议，在流行技术迭代中制造成本、功耗影响很大。mac pro最高140GB/s内存带宽
   - DDR SDRAM：同步动态随机存取存储器
   - DDR1：dual data rate，双边沿传输数据，在SDRAM基础上发展，所以MT/S是MHz的两倍。
   - DDR2
   - DDR3
     1. LPDDR3：双通道，2133MHz，读/写31GB/s，复制29GB/s，延迟100ns，Low Voltage(低电压版)
   - DDR4：最高4266MT/s，2015年，LPDDR4速率为3200MT/s，理论速度25.6GB/S，intel的傲腾2代内存为3200MT/s，目前某些在售服务器DDR4为2666MT/s
     1. LPDDR4
     1. LPDDR4
     1. 单通道，2666MHz，读/写/复制17GB/s，延迟90ns
     1. 双通道，2400MHz，读/写30GB/s，复制27GB/s，延迟70ns
     1. 双通道，2600MHz，读/写37GB/s，复制33GB/s，延迟57ns
   - LPDDR5：6400MT/s
1. 特性
   - 零拷贝
     1. 认识：用户空间无拷贝
        - 之前是磁盘io ——— 内核页缓冲区  ——— 用户缓冲区 ——— 内核socket缓冲区 ——— 网卡缓冲区
        - 现在直接从磁盘io到网络io，用文件描述符控制就行，是用户空间零拷贝
        - 4步变2步，不仅节省了大量文件拷贝，而且节省用户上下文切换
     1. 零拷贝方式：linux的sendfile、mmap、splice
        - 用户态直接I/O
        - 写时复制：fork时父子进程共享同一内存，父进程修改内存时，操作系统将该数据复制一份，以保证子进程的数据不受影响
   - 局部性原理：不论存取数据和指令，都趋于聚集在一个较小的连续的区域
1. wiki
   - 内存频率越高，内存读取越快
   - 32位一般是4K，cpu支持最大2的32次方，即4G的寻址空间
   - page(内存)=>块(文件系统)=>扇区(磁盘)
   - cpu只认内存地址，不关心单元空间、如何分布，硬件设计时保证了内存单元的地址和空间是对应的，如0x0021代表内存上的第0x0021个单元格，容量是一个字节
   - linux内核为充分利用物理内存，会在适当时候将物理内存中不常用的数据块自动交换到虚拟内存中，以保持尽可能多的空闲物理内存，即使并没有什么事情需要内存，Linux也会交换出暂时不用的内存页面，可以避免等待交换所需时间
### 网络
1. 网卡冗余：提高带宽、可用性
1. 网络带宽、网络质量
1. pps：每秒包数量，几百万量级
### 显存
1. 认识：帧缓存，用来存储显卡芯片处理过或即将提取的渲染数据，mac pro的显存带宽1TB/s
   - GDDR5
### 总线
1. 总线
   - 分类
     1. 第一代：ISA、EISA、VESA、Micro Platforms
     1. 第二代：PCI、AGP、PCI-X
     1. 第三代：3GIO，经PCI-SIG组织认证发布为PCI-Express
   - 介绍
     1. PCIe：PCI-Express，用来取代PCI
        - PCIe3.0：即Gen3，速率8GT/s，这是每一个位宽的速率，可以有多个如16个，x8比M.2快，mac pro有64条PCIe通道
        - PCIe4.0：速率16GT/s，2019年5月消费级设备发布
        - PCIe6.0：标准已发布
1. 分类
   - 地址总线：指定cpu将要操作的内存地址
   - 数据总线：读写内存的数据
   - 控制总线：发送和接收信号，比如中断、设备复位等，cpu收到后进行响应
### 操作系统
1. 操作系统
   - 功能：图形窗口、命令行、系统调用
     1. 统一管理计算机资源，并实现了抽象
     1. 提供了用户和计算机的接口
   - 类别
     1. 无操作系统：人工输入
     1. 批处理：无需等待人工操作、批量输入任务
     1. 分时系统：人机交互、即时调试程序
   - 分类
     1. MS-DOS
        - 认识：微软磁盘操作系统，是早期windows系统用的一种硬盘操作系统，就是提供硬盘的访问功能
        - 历史
          1. 1980，微软购买一名24岁程序员写的86-DOS的全部版权，并更名为MS-DOS。买下版权，推向市场
          1. 之后windows 1.x/2.x/3.x/9x/Me全部是基于MS-DOS的图形用户界面程序，windows NT不需要dos
          1. windows2000(NT 5.0)/XP(NT 5.1)/Vista(NT 6)/7(NT 6.1)，才抛弃MS-DOS
     1. Macintosh
        - 1984年，其具备的全图形化操作界面设计，以及使用鼠标操作的方式，为操作系统产业界带来革命性的概念。微软仍还停留在DOS文字界面
        - 1988年，IBM终于在OS/2 1.1版套上的图形界面，并成为在Intel CPU上的首个多工操作系统。后来，微软利用了与IBM合作得来的系统开发经验和技术用来开发自己的Windows 3.0操作系统，荒废了与IBM的合作。IBM得知微软的盘算之后，便和微软分道扬镳，从此完全自主进行OS/2的后续版本更新
        - OS/2高稳定性，全世界第一个真正的全32位操作系统，稳定性远优于当时的所有竞争操作系统。Window 95使用了32/16位混合设计，用以兼容过去的老应用，消费者纷纷投向Windows平台。而虽然后续的Windows 98/98SE与ME就只是个金玉其外的垃圾，尤其是后二者，在正常使用下一段时间后就会自我崩溃，使用者往往不得不隔一段时间就重新安装，稳定性极差
     1. OS/2：Operating System/2，IBM设计、微软协助开发的操作系统，受DOS在PC上的巨大成功影响而开发。2005年停止开发
     1. NT：New Technology，从零设计 从零开发的一个操作系统
1. 运行态
   - 理解：当一个进程执行系统调用而陷入内核代码中执行为内核态，其他为用户态，内核可以直接访问所有内存和外设，用户不可以
     1. 用户态不能干扰内核态，所以有了特权指令和非特权指令，内核部分使用特权指令
     1. 当系统调用返回时，CPU将用户进程从内核态切换回用户态
   - 切换方式
     1. 用户态到内核态
        - 系统调用：用户态进程申请使用操作系统提供的服务程序完成工作，即主动要求，如fork，操作系统为用户特别开放的一个中断
        - 异常：用户态程序执行异常，发生切换到处理此异常的内核相关程序中
        - 外围设备的中断：外围设备向CPU发出中断信号，这时CPU会暂停执行下一条即将要执行的指令转而去执行与中断信号对应的处理程序，如果先前执行的指令是用户态下的程序，就发生了由用户态到内核态的切换。如dla告诉cpu说数据准备好了
   - 常见内核态
     1. 进/线程管理
     1. 低级存储器管理：硬盘、内存
     1. 中断和陷入管理：中断、异常
1. 内存
   - 分类
     1. 内核空间：用于避免用户进程直接操作内核，保证内核安全
        - 进程私有的虚拟内存：每个进程都有单独的内核栈、页表、task 结构以及 mem_map 结构等
        - 进程共享的虚拟内存：属于所有进程共享的内存区域，包括物理存储器、内核数据和内核代码区域
     1. 用户空间：堆栈、代码段、数据段等
   - Linux I/O读写方式
     1. I/O中断：由CPU自身负责数据的传输过程，需要CPU中断。每次进程读取磁盘数据时都需要CPU中断，然后发起I/O请求等待数据读取和拷贝完成，每次的I/O中断都导致CPU的上下文切换。2次CPU拷贝，4次上下文切换。用户空间和内核空间的内存数据来回复制，才能下一步操作
        - 用户进程向 CPU 发起 read 系统调用读取数据，由用户态切换为内核态，然后一直阻塞等待数据返回
        - CPU 接收到指令后对磁盘发起I/O请求，将磁盘数据先放入磁盘控制器缓冲区
        - 数据准备完成以后，磁盘向CPU发起I/O中断
        - CPU 收到I/O中断后将磁盘缓冲区中的数据拷贝到内核缓冲区，然后再从内核缓冲区拷贝到用户缓冲区
        - 用户进程由内核态切换回用户态，解除阻塞状态，然后等待CPU的下一个执行时间钟
     1. 轮询
     1. DMA：Direct Memory Access，直接内存存取，不走内核，传输n个扇区到内存
### 指标梳理
1. 网络
   - 阿里云到世纪互联带宽：5G
   - 世纪互联IDC机房出口带宽
     1. 71万，占用最高1.9G
     1. 14G - 支持200万
     1. 20G - 支持300万
     1. 70G – 支持1000万
   - 专线带宽：1G/根 * 2
   - 阿里云CDN：1T
1. 服务器
   - 物理机
     1. 品牌：dell、lenovo、hpe
     1. 配置
        - DELL：R440
        - DELL：R420
        - 联想：SR530
        - 联想：SR570
          1. 3个PCIe 3.0
          1. 2*4114：24核
          1. 256G：2666MHz，ddr4
          1. 4*960G：标配4个3.5英寸硬盘
          1. 电口：2*1000M，光口：2*10G含模块
          1. RAID10 5：标配raid软件，还有Raid卡
          1. 2*550W 电源
1. 价格
   - 服务器：单台服务器加个为7W，单台实体机可以虚拟化成7台8核虚机
   - 机架租赁：10U的机柜，7500机柜/月
1. 架构
   - 网关层
     1. HA
     1. API
     1. HA lvs
     1. API lvs
     1. 切流网关：ECS+SLB
     1. vanrish
1. 云服务提供商
   - aliyun
   - window azure
   - amazon aws
1. 比特单位
   - kb、mb、gb：硬盘中以1000为一进制
   - kib、mib、gib：IEC规定的按照1024为单位的
1. 接口性能
   - kvm：10台8核16G，5-6次redis，qps为5到6万
1. 性能指标
   - 延迟数字：主要看比例，l1缓存访问0.5s，读内存2分钟，发送2kb 5小时，读SSD2天，读机械硬盘1MB 8个月，数据包来回5年
     1. cpu：SRAM，集成度低、功耗高、生产成本高
        - 计算：3GHz的大多数简单指令要一个时钟周期即三分之一ns
        - 寄存器：300ps
        - l1缓存访问：1ns
        - l2缓存访问：4ns
        - l3缓存访问：12ns
        - 分支预测错误：5ns
        - 互斥锁/解锁：25ns
     1. 内存：DRAM，需要周期性刷新电路，读写速度慢，但集成度高、功能低、生产成本低
        - cpu访问一次内存：60~100ns
        - 内存中连续读取1mb：250µs
        - 操作带宽：7000MBps
     1. 网络
        - 通过1Gbps发送2kb：20µs
     1. 存储
        - SSD随机读取：150µs
        - SSD连续读取1mb：1ms
        - 机械磁盘寻道：10ms
        - 机械磁盘连续读取1mb：20ms
     1. 其他
        - 压缩1kb：3µs
        - 同一个数据中心的来回：0.5ms
   - 技术名
     1. 雷雳3：USB-C，最高40Gb/s
     1. USB 3.1：最高10Gb/s
   - 侧重点
     1. cpu：计算读书
     1. 内存：带宽、延迟
     1. 硬盘：IOPS、IO时延
   - 测试工具
     1. sysbench：模块化、跨平台、多线程的基准测试工具，可单独测试cpu，压测方法为计算素数
        - 命令：`sysbench cpu--cpu-max-prime=20000 --threads=8 --time=50 run`
     1. Stream：业内公认的内存性能评估的基准测试工具，包括Copy(复制)、Scale(乘法)、Add(加法)、Triad(三者复合)四种不同操作情况下的内存带宽表现
     1. MLC：Intel官方提供的免费内存测试工具，可以有效方便的测试内存延时
     1. FIO：libaio引擎，块大小配置为4k，测试生成的文件大小为200G
     1. Netperf：主要针对基于TCP或UDP的传输，使用UDP协议+小包，小包大小为1byte
### 服务梳理
1. php+nginx
   - php扩展、ini参数
   - nginx的vhost，即server
1. 配套服务
   - rsync
   - confd、confd-tw
   - filebeat
   - flcon-agent
   - supervisor
1. 网关upstream
1. 发布系统
1. 日志收集：php、nginx
   - basiclog.xesv5.com
   - logcenter.xesv5.com
1. 机器监控
   - mao.xesv5.com
   - xesfalcon.xesv5.com
   - datamap.xesv5.com
     1. 网关监控：xesmap.xesv5.com/d/api-gw/api-gw?orgId=1
     1. k8s物理机：app.xesv5.com/k8s-core.grafana/d/fa49a4706d07a042595b664c87fb33ea/nodes?orgId=1
   - fengniao.xesv5.com/admin/#/loadshow
1. 耗时分析
   - 专线质量
     1. 延时 <=2ms，1.5ms，2.5ms
     1. 丢包：<=0.03%
     1. 带宽：10G x 4，多线路冗余自愈
   - 网校网关：一半多在1ms及以下，2ms、3ms依次减少，三者占99%
   - 容器网关：1ms以下
   - pdo连接数据库：1.5ms
   - redis连接：0.3ms
   - 走索引单条sql查询：0.6ms
   - 一次api请求：50ms、30ms等，几十毫秒，大于100ms的都是慢的
     1. dns解析：0.8ms、1ms
     1. connect_time：1.2ms
     1. pretransfer_time：1.4ms
   - 一次api请求：7ms
     1. dns解析：0.01ms、0.02ms
     1. connect_time：0.1ms
     1. pretransfer_time：0.1ms
     1. starttransfer_time：6.9ms
   - 整个php返回字符串请求耗时：6ms
     1. php处理时间：1.3ms
1. 架构
   - 配置：etcd+confd
   - supervisor
