### office
1. 解决方案提供者
   - office官方文档
   - openoffice
   - xdocin
1. 查看
1. 新增
1. 编辑
1. 转换
   - 聚合数据文档转换：https://www.juhe.cn/docs/api/id/259
   - 金山格式转换客户端
   - Aspose：收费
   - Office Web Apps：自行搭建服务
1. 操作
   - JavaCOMBridge：java的jacob包，调用COM对象
   - JCom：类似jacob
   - POI：apache旗下，Java API for Microsoft Documents
   - openoffice：apache旗下，
   - libreoffice：自由免费
### PDF
1. 理解：Portable Document Format，便携式文档格式，Adobe公司开发的可移植文档格式，以PostScript语言图象模型为基础，保证打印机无关的精确打印效果
1. 特点
   - 跨平台，与应用程序、操作系统、硬件无关，流行范围广
   - 多媒体集成，可将字形、独立于设备和分辨率的图形图像封装在文件中随文件传输，文件压缩效率高
   - 阅读效果好
   - 存在页的概念
1. 格式组成：首部、文件体(有若干obj对象组成)、交叉引用表、尾部
1. 历史：1990年代早期开发，版本有1.1~1.6(05年)
1. wiki：Adobe公司, 是著名的图形图像和排版软件的生产商
### 语言框架区别
1. .net - webform，.net mvc, .net core2
   - 所有微软出品的东西都是一样的感觉：很贴心，很温暖，
   - 但前提是你得住他家，所有的东西都要用他家的
1. java - springboot
   - 很方便，很强大，就是出了问题不知道在哪，控制台日志能看晕
   - 就像开着坦克上高速的感觉
1. python - flask
   - 写起来很爽、部属愁死人
1. js - express/koa
   - 好写好部属，库不要太多
   - 无静态检查的语言让我们这些java、.net程序员怎么放心的写啊？--typescript或许是解决办法？
   - 总感觉像闹着玩，怎么写都行...这玩意丢到线上真不会挂掉？
1. 压测表现
   - php
     1. 配置：4核 xeon 2.2，12G，fpm数 500，kvm+centOS7.6
     1. 表现
        - fpm空跑hello：1w
        - composer autoload：7000
        - fend框架内echo：5000
        - swoole 1.10.x：2.4w
### other
1. 图像相识度
   - 识别依据：颜色特征、纹理特征、形状特征和空间关系特征等
   - 方法
     1. 图片划块，分别计算颜色特征，划块后准确率高一些
     1. 感知哈希算法
     1. 统计直方图，复杂一点的 surf，sift算法
     1. sift：通过复杂的数据公式实现物体局部特征提取，具有平移、旋转、光照不变性
     1. surf：采用了SIFT相近的实现原理，但计算复杂度降低很多
     1. 文理特征：提取图片中颜色渐变与物体纹理数据特征，用于物体分类、图像搜索
     1. 学下图像识别，分割，运动追踪
     1. 要是目标跟踪，有自己的一套理论，可以一步步迭代算法，加上马尔可夫或者卡尔曼预测，邻域搜索
     1. opencv
1. 色情图片识别：识别皮肤块裸露程度
### 白板
1. 硬件采点
   - InkCanvas：微软
   - Wintab：闭源，授权方式
1. 平滑处理
   - 多点加权平均
   - 贝塞尔曲线：一种平滑处理技
### 区块链
1. 认识：单向链式存储的区块，用加密技术保证不可修改，是一个不断增长的全网总账本。看作只能追加的分布式只读数据库，源于比特币
   - 创建了无需信任中心的货币发行机制。分布式记账，保证每笔交易可信并不可篡改。整个账本全部公开，人手一份，全网相同。也可用于数字身份验证，清算业务等传统的必须由第三方介入的业务，从而降低交易成本
   - 发行数量由程序决定，无法随意修改
   - 交易账本完全公开可追溯，不可篡改。天生可验证、可信任
   - 密码学理论(hash算法)保证货币防伪造，防双花
   - 数字签名机制保证交易完整可信，不可抵赖和撤销
     1. 匿名性：只知道公钥，并不知道对应的私钥，也不知道私钥是谁的
1. 组成
   - 区块：哈希算法保证
     1. Merkle Hash：记录了本区块所有的交易，通过交叉计算hash，交易的每个字节、顺序都可以保证。改区块Merkle Hash通不过，伪造区块链需要拥有超过51%的全网算力
     1. Block Hash：区块哈希，区块本身，可以追踪上一个区块。整个区块依靠Block Hash确保区块无法修改
   - 交易：数字签名保证零信任的可靠P2P交易，通过执行解锁脚本确认某个UTXO的资产可以被私钥持有人转移给其他人，没有账户这个概念
     1. 所有交易被记录在区块链中，可以通过公钥查询所有交易信息
     1. 多个输入输出，差额就是隐含的交易费用，会算入当前区块的矿工收入中作为矿工奖励的一部分
     1. 私钥是花费比特币的唯一手段，丢失了就永远无法花费对应公钥的比特币。私钥本质上就是一个256位整数，对私钥进行WIF格式编码可以得到一个带校验的字符串。有压缩和非压缩格式
     1. 公钥
        - 公钥是根据私钥计算出来的，有压缩和非压缩两种表示方法，可互相转换
        - 比特币的地址是公钥哈希的编码，并不是公钥本身，通过公钥可推导出地址，任何用户都可以通过公钥查询到某个交易的输入和输出金额
        - 通过地址不可推导出公钥，通过公钥不可推导出私钥
   - HD钱包：依托钱包软件，帮助用户管理私钥。采用分层确定性算法通过根扩展私钥计算所有层级的所有子扩展私钥，继而得到扩展公钥和地址
1. 完善性保证措施
   - 工作量证明机制保证修改区块链的难度非常巨大从而无法实现，巨大算力基础上提供保护不可篡改特性。比特币总量被限制为约2100万，初始挖矿奖励为每区块50个，每4年减半
     1. 比特币的网络安全实际上就是依靠强大的算力保障的
     1. 小明想要把某个公钥地址的输出支付给小红，他就需要使用自己的私钥对这笔交易进行签名，而矿工验证这笔交易的签名是有效的之后，就会把这笔交易打包到区块中，从而使得这笔交易被确认。比特币交易的输出一个锁定脚本，下一个交易的输入是一个解锁脚本
1. wiki
   - 货币
     1. 电子货币
     1. 数字货币：比特币人类历史上第一种数字货币
   - 比特币：第一个基于数学加密原理构建的分布式数字货币系统，使用区块链技术实现了数字货币的可信支付，解决了现金电子化以后交易的清结算问题。08创建，
     1. 创世区块
     1. 传统的依赖权威的中央数据库，中央数据库不会修改
   - p2p交易原理
     1. 数字签名：防伪造，防篡改，防抵赖
     1. 私钥证明自己，公钥作为转账账号
   - 挖矿原理：工作量证明机制，p2p网络中，不停进行计算的节点，试图把新的交易打包成新的区块并附加到区块链上，这类节点就是矿工。每打包一个新的区块，打包该区块的矿工就可以获得一笔比特币作为奖励。所以，打包新区块就被称为挖矿
     1. 谁算的小算谁的，然后通过共识算法得到大家确认
     1. 有递减规则，4年减半
   - 可编程支付：转账方用自己私钥和对方公钥加密锁定脚本，接收方自己私钥解密脚本，就是通过私钥转移花费的权利，支付的本质是由程序触发的数字资产转移，无需信任中介参与零信任
     1. 多重签名：降低丢失风险，可以联合投票动用资金，n个人持有私钥，m个人同意可花费
     1. UTXO模型
     1. Segwit地址：隔离见证地址
        - 比特币延展性攻击的原因是ECDSA签名总是有两个有效签名，且可以相互计算；
   - 以太坊：支持智能合约的区块链平台，与比特币最大的不同是通过一个虚拟机（EVM）可以运行智能合约。任何人都可以编写合法的图灵完备脚本来执行任意逻辑（有很多限制），例如，定义一种新的代币，抵押贷款等
### 推送
1. 消息类型
   - 通知：通知栏的消息，能够在开机联网的情况下实时到达，不要求APP保活
   - 透传：通常不会展示到通知栏上，由应用解析消息内容执行逻辑，需要APP在线、保活
1. 推送实现方式
   - PULL：拉，轮询，不实时，频繁了压力大
   - PUSH：推，建立长连接，实时性好，保持的大量长连接比较占用资源
   - SMS：拦截SMS消息并解析，需要给移动公司交钱，属于推，不合适
1. 推送平台
   - 官方：iOS APNS，Android GCM
     1. 只维护一个长连接，省电、安全、快速
   - 第三方平台：个推，极光，友盟，信鸽
     1. 随着android系统权限的收缩，很少有应用可以在后台活着了，各大厂已经形成了自己的体系
     1. 应用派系中报团相互唤醒，实现使用同一推送间相互唤醒
   - 手机厂商自带的系统级
1. 推送问题
   - 同一个APP用户切换账号：要更新userId和clientId绑定关系，否则推送错误
   - 应用卸载重装：重新生成clientId，防止误推
### 动画
1. 动画帧：如关键帧、空白帧和补间动画
1. 制作工具
   - ppt
   - Synifg Studio：免费开源，2D矢量动画图形的创建
   - Adobe Animate：flash改名，用于矢量图形和动画
   - FlipaClip：逐帧动画工具，通过多帧图片自动合成一张动图或者短视频
   - 专业的：3DSMax / Maya / C4D / Blender